package panels;

import javax.swing.*;
import java.awt.*;

public class ContentPane extends JPanel {

	public ContentPane(Dimension size) {
		super();
		setSize(size);
		setLocation(0, 0);
		setLayout(null);
	}
}